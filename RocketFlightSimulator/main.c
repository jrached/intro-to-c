/* Create a program that predicts the height of a rocket using the equation: X(t) = VMAX*((2/a)*ln((e^(a*t) + 1)/2) - t)),
where VMAX is the maximum velocity of the rocket as t->oo, t is time, X(t) is position, and a is a constant.
*/

#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    printf("Hello world!\n");
    return 0;
}
